Run via Updater.py. Be sure to update the directories.

Package Dependencies:
- xlrd (for reading the excel files, openpyxl wouldn't open them)
- openpyxl (for writing to excel)
- validate_email (as the name implies)